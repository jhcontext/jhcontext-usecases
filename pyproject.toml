[project]
name = "jhcontext-usecases"
version = "0.1.0"
description = "PAC-AI compliance scenarios: Healthcare (Art. 14) and Education (Art. 13)"
requires-python = ">=3.10"
dependencies = [
    "jhcontext>=0.5.0,<0.6",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
